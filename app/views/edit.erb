<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h3><%=  @post.name %></h3>
        <%=  @post.content %>
    <form action="/posts/<%=@post.id%>" method="POST">
      <input id="hidden" type="hidden" name="_method" value="patch" />
      <input value="<%=@post.name%>" id="name" name="post[name]" />
      <input value="<%=@post.content%>" id="content" name="post[content]" />
      <button type="submit">submit</button>
    </form>
  </body>
</html>
